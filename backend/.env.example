# Backend Environment Configuration
PROJECT_NAME="RailOne API"
VERSION="1.0.0"

# Secret Key for JWT token signing
SECRET_KEY="railmate-super-secret-jwt-key-2026-production-ready"

# Database connection:
# Local SQLite default:
DATABASE_URL="sqlite:///./railmate.db"
# Production PostgreSQL (e.g. Render / Neon / Supabase):
# DATABASE_URL="postgresql://user:password@host:5432/railone"

# Allowed CORS Origins (comma-separated for multiple)
BACKEND_CORS_ORIGINS="https://rail-mate-ochre.vercel.app,http://localhost:5173,http://localhost:3000"
